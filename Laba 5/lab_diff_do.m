function [df,t] = lab_diff_do(a,b,n,name,k,f)
    switch name
        case 'forward'
            
        case 'backward'
            
        case 'central'
            
        otherwise
            df=0; t=0;
    end
end